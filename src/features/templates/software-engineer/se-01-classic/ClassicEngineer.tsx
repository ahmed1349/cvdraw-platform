import { ThemedCV } from "@/features/templates/engine/ThemedCV";
import type { CVTemplateProps } from "@/features/templates/types";

export default function ClassicEngineer(props: CVTemplateProps) {
  return <ThemedCV {...props} />;
}
